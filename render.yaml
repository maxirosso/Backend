services:
  - type: web
    name: ecommerce-backend
    env: node
    plan: free
    buildCommand: npm ci --only=production
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
    
